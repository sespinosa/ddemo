import { roundRectangle } from '../../utils/shapes'
import { renderBtnText, renderBtnImg } from './button'
// import logo from './logo_bottom'
import sprites from '../../utils/sprites'

const renderLayer = (props) => {
  roundRectangle(props)
}

export { renderLayer }
